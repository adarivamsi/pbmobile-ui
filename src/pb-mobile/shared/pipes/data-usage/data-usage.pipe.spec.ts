import { DataUsagePipe } from './data-usage.pipe';

describe('DataUsagePipe', () => {
  it('create an instance', () => {
    const pipe = new DataUsagePipe();
    expect(pipe).toBeTruthy();
  });
});
